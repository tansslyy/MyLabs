<Window x:Class="lab6.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Записна книжка" Height="450" Width="650"
        Loaded="Window_Loaded">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <ListBox x:Name="NotesList" Grid.Column="0" Margin="0,0,10,0"
                 DisplayMemberPath="NoteText"
                 SelectionChanged="NotesList_SelectionChanged"/>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBox x:Name="NoteTextBox" Grid.Row="0"
                     TextWrapping="Wrap" AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto" Margin="0,0,0,10"
                     FontSize="14" Padding="5"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnAdd" Content="Додати новий" Width="110" Margin="5" Padding="5" Click="BtnAdd_Click"/>
                <Button x:Name="BtnSave" Content="Зберегти зміни" Width="110" Margin="5" Padding="5" Click="BtnSave_Click"/>
                <Button x:Name="BtnDelete" Content="Видалити" Width="110" Margin="5" Padding="5" Click="BtnDelete_Click" Background="#FFFA8A8A"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>